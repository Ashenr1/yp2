<UserControl x:Class="UP02.Elements.ItemConsumables"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E41613"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#0060AC"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White" Margin="10" Padding="15" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Фото -->
            <Border Grid.Column="0" Background="#F0F0F0" CornerRadius="10" Height="100" Width="100" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,10,0">
                <Image x:Name="Photo" Stretch="Uniform"/>
            </Border>

            <!-- Основная информация -->
            <StackPanel Grid.Column="1" Margin="10,0">
                <TextBlock x:Name="NameLabel" Text="{Binding Name}" FontWeight="Bold" FontSize="15" Foreground="#0060AC"/>
                <TextBlock x:Name="DescriptionLabel" Text="{Binding Description}" FontSize="12" Foreground="#666" TextWrapping="Wrap" Margin="0,5,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="📦 Количество:" FontSize="12" Foreground="#0060AC" FontWeight="SemiBold" Width="80"/>
                    <TextBlock x:Name="QuantityLabel" Text="{Binding Quantity}" FontSize="12" Foreground="#333"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🏷 Тип:" FontSize="12" Foreground="#0060AC" FontWeight="SemiBold" Width="80"/>
                    <TextBlock x:Name="TypesConsumableLabel" Text="{Binding TypeConsumables.Type}" FontSize="12" Foreground="#333"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅 Поступление:" FontSize="12" Foreground="#0060AC" FontWeight="SemiBold" Width="80"/>
                    <TextBlock x:Name="ReceiptDateLabel" Text="{Binding ReceiptDateString}" FontSize="12" Foreground="#333"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="👤 Ответственный:" FontSize="12" Foreground="#0060AC" FontWeight="SemiBold" Width="80"/>
                    <TextBlock x:Name="ResponsibleUserLabel" Text="{Binding ResponsibleUser.FullName}" FontSize="12" Foreground="#333"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔄 Вр. ответственный:" FontSize="12" Foreground="#0060AC" FontWeight="SemiBold" Width="80"/>
                    <TextBlock x:Name="TempResponsibleUserLabel" Text="{Binding TempResponsibleUser.FullName}" FontSize="12" Foreground="#333"/>
                </StackPanel>
            </StackPanel>

            <!-- Характеристики и история -->
            <StackPanel Grid.Column="2" Margin="10,0">
                <TextBlock Text="📋 Характеристики" Style="{StaticResource SectionTitleStyle}"/>
                <DataGrid x:Name="CharacteristisDG" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MaxHeight="120" BorderBrush="#E0E0E0" BorderThickness="1" RowHeight="28">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" Binding="{Binding CharacteristicName}" Width="*"/>
                        <DataGridTextColumn Header="Значение" Binding="{Binding Value}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="📜 История ответственных" Style="{StaticResource SectionTitleStyle}"/>
                <DataGrid x:Name="responsibleUserHistory" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" MaxHeight="120" BorderBrush="#E0E0E0" BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding ChageDateString}" Width="*"/>
                        <DataGridTextColumn Header="Ответственный" Binding="{Binding OldUser.FullName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="10,0,0,0">
                <Button Click="DeleteClick" Background="#9EA1A2" Style="{StaticResource IconButtonStyle}" ToolTip="Удалить">
                    <TextBlock Text="🗑"/>
                </Button>
                <Button x:Name="UpdateButton" Click="UpdateClick" Background="#0060AC" Style="{StaticResource IconButtonStyle}" ToolTip="Редактировать">
                    <TextBlock Text="✏"/>
                </Button>
                <Button Click="ExportConsumablesClick" Background="#9EA1A2" Style="{StaticResource IconButtonStyle}" ToolTip="Приём-передача">
                    <TextBlock Text="📤"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>