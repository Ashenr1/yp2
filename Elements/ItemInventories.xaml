<UserControl x:Class="UP02.Elements.ItemInventories"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E41613"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoRowStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White" Margin="10" Padding="15" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="📋 Название:" FontWeight="SemiBold" FontSize="13" Foreground="#0060AC" Width="140"/>
                    <TextBlock Text="{Binding Name}" FontSize="13" Foreground="#333"/>
                </StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="👤 Создал:" FontWeight="SemiBold" FontSize="13" Foreground="#0060AC" Width="140"/>
                    <TextBlock Text="{Binding User.FullName}" FontSize="13" Foreground="#333"/>
                </StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="📅 Дата начала:" FontWeight="SemiBold" FontSize="13" Foreground="#0060AC" Width="140"/>
                    <TextBlock Text="{Binding StartDateString}" FontSize="13" Foreground="#333"/>
                </StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="📅 Дата окончания:" FontWeight="SemiBold" FontSize="13" Foreground="#0060AC" Width="140"/>
                    <TextBlock Text="{Binding EndDateString}" FontSize="13" Foreground="#333"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="DeleteButton" Click="DeleteClick" Background="#9EA1A2" Style="{StaticResource IconButtonStyle}" ToolTip="Удалить">
                    <TextBlock Text="🗑"/>
                </Button>
                <Button Click="UpdateClick" Background="#0060AC" Style="{StaticResource IconButtonStyle}" ToolTip="Редактировать">
                    <TextBlock Text="✏"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>