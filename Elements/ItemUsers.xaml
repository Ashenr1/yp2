<UserControl x:Class="UP02.Elements.ItemUsers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E41613"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoRowStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White" Margin="10" Padding="15" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- ФИО и роль -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="👤" FontSize="24" HorizontalAlignment="Center"/>
                <TextBlock x:Name="LastName" Text="{Binding LastName}" FontWeight="Bold" FontSize="14" Foreground="#0060AC" TextAlignment="Center"/>
                <TextBlock x:Name="FirstName" Text="{Binding FirstName}" FontSize="13" Foreground="#333" TextAlignment="Center"/>
                <TextBlock x:Name="MiddleName" Text="{Binding MiddleName}" FontSize="12" Foreground="#666" TextAlignment="Center"/>
                <Border Background="#0060AC" CornerRadius="10" Padding="8,2" Margin="0,8,0,0" HorizontalAlignment="Center">
                    <TextBlock x:Name="Role" Text="{Binding Role}" FontSize="11" Foreground="White" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>

            <!-- Контактная информация -->
            <StackPanel Grid.Column="1" Margin="20,0">
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="🔑 Логин:" FontSize="13" Foreground="#0060AC" FontWeight="SemiBold" Width="70"/>
                    <TextBlock x:Name="Login" Text="{Binding Login}" FontSize="13" Foreground="#333"/>
                </StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="📧 Email:" FontSize="13" Foreground="#0060AC" FontWeight="SemiBold" Width="70"/>
                    <TextBlock x:Name="Email" Text="{Binding Email}" FontSize="13" Foreground="#333"/>
                </StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="📞 Телефон:" FontSize="13" Foreground="#0060AC" FontWeight="SemiBold" Width="70"/>
                    <TextBlock x:Name="Phone" Text="{Binding Phone}" FontSize="13" Foreground="#333"/>
                </StackPanel>
                <StackPanel Style="{StaticResource InfoRowStyle}">
                    <TextBlock Text="📍 Адрес:" FontSize="13" Foreground="#0060AC" FontWeight="SemiBold" Width="70"/>
                    <TextBlock x:Name="Address" Text="{Binding Address}" FontSize="13" Foreground="#333" TextWrapping="Wrap" MaxWidth="300"/>
                </StackPanel>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Click="DeleteClick" Background="#9EA1A2" Style="{StaticResource IconButtonStyle}" ToolTip="Удалить">
                    <TextBlock Text="🗑"/>
                </Button>
                <Button Click="UpdateClick" Background="#0060AC" Style="{StaticResource IconButtonStyle}" ToolTip="Редактировать">
                    <TextBlock Text="✏"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>